<!-- Project Delete 작업 Loading 표시 Dialog -->
    <UserControl x:Class="ProstMain.Dialog.ProgressBarDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:wpf="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
             
             mc:Ignorable="d" Background="DarkGray" Height="130" Width="200">

    <Grid>
        <GroupBox>
            <GroupBox.Header>
                <StackPanel Orientation="Horizontal">
                    <wpf:PackIcon Kind="WarningCircle" Foreground="Orange"/>
                    <TextBlock Text="Project Delete" Foreground="{DynamicResource ResourceKey=GroupBoxHeaderForeground}" Margin="8,0,0,0" FontWeight="Bold"/>
                </StackPanel>
            </GroupBox.Header>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="7*"/>
                    <RowDefinition Height="3*"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="5*"/>
                </Grid.ColumnDefinitions>

                <ProgressBar Grid.ColumnSpan="2" Style="{DynamicResource MaterialDesignCircularProgressBar}" HorizontalAlignment="Center"  Width="50" Height="50" IsIndeterminate="True" Value="0" />
                <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Foreground="Black" FontSize="13" Text="The Project is being deleted" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>

        </GroupBox>
    </Grid>

</UserControl>




