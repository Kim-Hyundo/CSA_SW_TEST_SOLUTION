<UserControl x:Class="GraphProject.Views.MemoryUsageContainerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GraphProject.Views"
             mc:Ignorable="d"
             xmlns:ViewModels="clr-namespace:GraphProject.ViewModel"
             xmlns:md ="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DesignHeight="884" d:DesignWidth="1490"
             d:DataContext="{Binding MemoryUsageContainerViewModel, Source={StaticResource Locator}}">
    <!--   <ContentControl Content="{Binding ContentView}"/> -->
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="12*"/>
        </Grid.RowDefinitions>

        <ContentControl x:Name="TotalContentView" Grid.Row="0" Grid.Column="0" Margin="5" Content="{Binding TotalContentView}"/>

        <StackPanel Grid.Row="1" Orientation="Vertical">
            <ContentControl Name="BottomContentView" Content="{Binding GridContainer,UpdateSourceTrigger=PropertyChanged}"></ContentControl>
        </StackPanel>

    </Grid>
</UserControl>
